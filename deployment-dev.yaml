apiVersion: apps/v1
kind: Deployment
metadata:
  name: mon-deployment
  namespace: dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mon-nginx
  template:
    metadata:
      labels:
        app: mon-nginx
    spec:
      containers:
        - name: nginx
          image: nginx